# MIP C# WinForms Exam — Comprehensive Practical (3 Variants)

This repository contains the **final practical exams** for the **MIP C# WinForms** module at  
**UMFST Târgu Mureș (Informatics, 2nd Year)**.

Each variant is a standalone project that evaluates **OOP**, **database**, **JSON handling**,  
and **WinForms GUI** skills through a real-world scenario.

---

## 🧭 Exam Variants Overview

| Variant | Topic | JSON Theme | Internet | Difficulty | GUI |
|----------|--------|-------------|-----------|-------------|------|
| 1 | **BookStore Manager** | Store, authors, books, orders, payments | Allowed | Medium–Hard | Mandatory |
| 2 | **Car Service Dashboard** | Garage, clients, cars, work orders, diagnostics | Allowed | Medium–Hard | Mandatory |
| 3 | **Warehouse Management (Offline)** | Products, suppliers, stock movements | **Offline / No AI** | Easy–Medium | Mandatory |

Each variant includes:
- `data_<variant>.json` — provided dataset with nested, partially invalid records  
- `README_<variant>.md` — exam instructions, structure, and grading rubric  
- Expected text report examples for output (`sales_report.txt`, `workorder_summary.txt`, `stock_report.txt`)

---

## 🎯 Learning Objectives

Students must demonstrate the following competencies:

- Reading and parsing JSON data in C# (`System.Text.Json` or `Newtonsoft.Json`)
- Designing OOP class structures for nested JSON data
- Building and connecting **databases** (ADO.NET or EF6 + SQLite)
- Implementing full **WinForms GUIs** with tabs, grids, filters, and reports
- Validating, skipping, and logging malformed data
- Applying CRUD, filters, and aggregations in code
- Managing database lifecycle (reset, reimport, export)
- Proper GIT usage with multiple commits

---

## 🧰 Technical Requirements

- **Language:** C# (.NET Framework 4.x)
- **IDE:** Visual Studio 2019 / 2022
- **Database:** ADO.NET or Entity Framework 6 (with SQLite or local MDF)
- **Environment:**  
  - Variants 1–2 → Internet allowed  
  - Variant 3 → Offline only (no internet, no AI tools)
- **External Libraries:**  
  - Allowed: ANY valid JSON parsing library, System.Data.SQLite, EF6  
  - Not allowed: Cloud APIs, external services, or AI assistance

---

## 📂 Repository Structure

```
/
├── Variant1_Bookstore/
│   ├── data_bookstore_final.json
│   └── README_Bookstore.md
│
├── Variant2_CarService/
│   ├── data_car_service.json
│   └── README_Car_Service.md
│
├── Variant3_Warehouse_Offline/
│   ├── data_warehouse.json
│   └── README_Warehouse_Offline.md
│
└── README.md   # This overview file
```

---

## 🧪 Grading (Each Variant)

| Criterion | Points |
|------------|--------|
| JSON parsing & validation | 2 |
| Database schema & import logic | 2 |
| GUI implementation (functional & interactive) | 3 |
| Logic & computations (filters, reports, summaries) | 2 |
| Reset & export functionality | 1 |
| **Total** | **10 points** |
| **Bonus:**  +2 for graphical statistics (chart, summary view) |

---

## ⚙️ Evaluation Rules

1. Each student is assigned one variant.  
2. Source code must compile and run in Visual Studio without modifications.  
3. GUI must be **fully functional** — console apps are not accepted.  
4. Any missing or broken functionality leads to point deductions per rubric.  
5. For Variant 3 (offline), AI tools and internet are strictly forbidden.

---

## 🧠 AI Usage and Naming Constraints (Exam Integrity Policy)

AI tools (ChatGPT, Copilot, Gemini, etc.) **may only be used** in **Variants 1 and 2**.  
Variant 3 must be completed **fully offline** without AI support.

---

### ✅ Permitted (Variants 1–2 only)
- Using AI to recall language syntax or API references.  
- Asking conceptual or debugging questions.  
- Generating *isolated* code fragments that you fully understand and adapt.

---

### ❌ Forbidden
- Entire WinForms or EF scaffolds generated by AI.  
- Code that includes meaningless variable names such as `obj`, `foo`, `bar`, `list1`, `dataX`, or generic `Form1`, `Class1`.  
- Classes or properties that do not correspond to fields inside the provided JSON.  
- Empty constructors or unused `Console.WriteLine()` calls inside a GUI project.  
- Any WPF/XAML, Blazor, or async web logic (WinForms only).  
- Code referencing cloud or OpenAI APIs, or HTTP JSON downloaders.  
- Copy-pasted comment blocks from GPT (e.g. `// create a new instance of the class`).  
- Identical naming patterns across student submissions (`Program.cs` with `Main` calling `Application.Run(new Form1())` and unused helpers).  

---

### 🔤 Mandatory Naming and Structure Rules

| Element | Requirement |
|----------|--------------|
| **Main Form name** | `MainWindow.cs` (not `Form1.cs`) |
| **Database context class** | Must be named `AppContext` (if using EF) |
| **Main namespace** | `UMFST.MIP.<VariantName>` |
| **Entity class names** | Must exactly match the JSON objects (e.g. `Book`, `Client`, `WorkOrder`, `Product`) |
| **Button names** | Must be semantic (e.g. `btnImport`, `btnExport`, `btnReset`, `btnFilter`) |
| **Event handlers** | Must follow `control_action` pattern (e.g. `btnImport_Click`) |
| **Output files** | Must use the names specified in README (`sales_report.txt`, `workorder_summary.txt`, `stock_report.txt`) |

Failure to follow these naming conventions will be interpreted as **non-compliance** or **AI-generated structure**.

---

### ⚖️ Penalties

| Violation | Penalty |
|------------|----------|
| Minor AI traces (comments, redundant code) | −2 pts |
| Major AI pattern (structure or naming mismatch) | −4 pts |
| Full AI project or inability to explain code | Grade 1 (Fail) |

---

_Repository maintained for educational use at the University of Medicine, Pharmacy, Science and Technology “George Emil Palade” Târgu Mureș — MIP C# WinForms module (Informatics, Year 2)._
